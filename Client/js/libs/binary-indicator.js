// Generated by CoffeeScript 1.9.3
(function() {
  var $, Binary, log;

  $ = require("jquery");

  log = console.log;

  Binary = (function() {
    function Binary(id, key1, client1) {
      this.id = id;
      this.key = key1;
      this.client = client1;
      this.init_dom_element();
      this.init_update_iteration(500);
    }

    Binary.prototype.init_dom_element = function() {
      this.element = document.createElement('div');
      $(this.element).attr("id", this.id).appendTo('.binary').text(this.id);
      this.indicator = document.createElement('div');
      return $(this.indicator).attr("id", this.id + "_ind").addClass("false").appendTo("#" + this.id);
    };

    Binary.prototype.init_update_iteration = function(time) {
      var client, element, key;
      element = $(this.indicator);
      key = this.key;
      client = this.client;
      return window.setInterval(function() {
        return client.getAsync(key).then(function(res) {
          if (res === "true") {
            return element.removeClass("false").addClass("true");
          } else {
            return element.removeClass("true").addClass("false");
          }
        });
      }, time);
    };

    return Binary;

  })();

  exports.Binary = Binary;

}).call(this);
